<!-- Content Heading -->
<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>


        <!-- Flash message -->
        <% if (messages.notice) { %>
            <div class="flash-message">
                <%= messages.notice %>
            </div>
        <% } %>

<%# messages() %>


<!-- Inventory Items -->

<%- grid %>